#Docker run will list all the containers which are running
docker ps
docker container ls
docker container ls -a
#Lists all images which are present in our system.
  docker images
#
docker pull <image_name>
#Docker Build 
  docker build -t image_name:tag .
#run the image 
docker run -d --name container-xx image-xx

  
